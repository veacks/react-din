@AGENTS.md

## Claude-specific notes

- Open the relevant playbook under `project/skills/` before large edits (components, schema, coverage).
- When editing `docs/components/**`, keep the required section headings in `AGENTS.md` in the same order.
